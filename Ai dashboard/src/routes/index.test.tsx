import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';

describe('Basic Test', () => {
  it('should pass a basic truthy test to verify test setup works', () => {
    expect(true).toBe(true);
  });
});
